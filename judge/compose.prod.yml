# Production compose for judge-worker VPS.
# No local Postgres — points at managed DB (e.g. Neon) via .env's DATABASE_URL.
# Usage: docker compose -f compose.prod.yml up -d --build
services:
  judge-worker:
    build: .
    cap_add:
      - SYS_ADMIN
      - NET_ADMIN
    env_file:
      - .env
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
